# Coding Agent Primitives

This provider-free example creates a temporary Git repository and demonstrates bounded workspace access, patching, Git status, and confirmation metadata.

Run from `framework/`:

```bash
uv run python examples/18_coding_agent/01_deterministic_primitives.py
```
